
import {Animal} from "./classes/Animal";


let boxShadowLink;
let preView: HTMLElement;

// function labelReplacer(el: PointerEvent): void {
//   console.log(el)
//   const target = el.target as Element;
//   const label: string = target.innerHTML;
//   const labelParts: string[] = label.split('-')
//   labelParts.splice(1, 1, `${staticCounter}`);
//   target.innerHTML = labelParts.join('-');
// }
